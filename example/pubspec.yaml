name: dropdown_kit_example
description: Example app for the dropdown_kit package.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dropdown_kit:
    path: ../   

flutter:
  uses-material-design: true
